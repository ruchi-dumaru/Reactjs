# spread operator
project-3
     ```let newObj={...obj};
   newObj.name="suzane dumaru"
   setObj(newObj);
      ``` 

  `setObj(prev=>({...prev,age:90}));`

 both are same for copying and ipdating the things   

 # Value increae by 2 
    ```setObj(prev=>(prev+1))
    setObj(prev=>(prev+1))```
     